{
  "name": "language-astronauts-cocos",
  "private": true,
  "version": "0.1.0",
  "scripts": {
    "test": "vitest",
    "test:run": "vitest run",
    "typecheck:test": "tsc -p tsconfig.test.json --noEmit"
  },
  "devDependencies": {
    "typescript": "^5.8.3",
    "vitest": "^3.2.4"
  }
}
